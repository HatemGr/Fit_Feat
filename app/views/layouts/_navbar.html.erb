<%if current_user %>
  <div class="ghost-div">
    <div class="navbar">
      <div>
        <div class="user-name">
          <div class="rond ml-2">
            <%if current_user.avatar.attached?%>
              <%= image_tag current_user.avatar, alt: 'avatar', :style => "width:50px;" %>
            <%else%>
              <%= image_tag "defaultavatar.png", :style => "width:50px;" %>
            <%end%>
          </div>
          <% if current_user.first_name%>
          <span>  <%= link_to current_user.first_name, edit_user_path(current_user)%> </span>
          <%else%>
            <span>  <%= link_to "Toi", edit_user_path(current_user)%> </span>
          <%end%>
        </div>
        <div class="separate-div" style="margin-left: 37px"></div>
        <nav class="menu" role="tablist">
          <% if current_page?(user_path(current_user))%>
            <%= render "layouts/tabs/home_tab" %>
          <% elsif current_page?(user_connections_path(current_user))%>
            <%= render "layouts/tabs/friends_tab" %>
          <% elsif current_page?(sport_user_path(current_user))%>
            <%= render "layouts/tabs/sport_user_tab" %>
          <% elsif current_page?(events_path)%>
            <%= render "layouts/tabs/events_tab" %>
          <% elsif current_page?(messages_path)%>
            <%= render "layouts/tabs/messages_tab" %>
          <% else %>
            <%= render "layouts/tabs/inactive_tab" %>
          <% end%>
        </nav>
      </div>
      <div class="nav-down">
        <div class="separate-div"></div>
        <div class="bottom-link">
            <i class="fa-solid fa-gear"></i>
             <%= link_to "Editer son profil", edit_user_path(current_user) %>
        </div>

        <div class="bottom-link">
            <i class="fa-solid fa-arrow-right-from-bracket"></i> 
             <%= link_to "Se déconnecter", destroy_user_session_path, :method => :delete %>
        </div>
      </div>
    </div>
  </div>
<%end%>
